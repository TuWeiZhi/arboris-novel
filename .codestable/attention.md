# Attention

每次会话必读的项目事实。

- 改进计划与全量设计方案：`docs/improvement-plan.md`（治理闭环 + 检索增强 + 遗留，①—⑩）。
- 进行中的工作游标：`.codestable/work/feat-governance.md`（含进度/状态/未决，恢复工作时先读）。
- 关键事实：角色状态读取真源是 `ProjectMemory.extra["raw_state_text"]`（文本 blob），非 `CharacterState` 表。
